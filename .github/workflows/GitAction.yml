name: Run Xunit Text
on: 
  push:
    branches:
      - Xunit_Dev

jobs:
  Test:
    
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up .Net Env
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '6.x'

      - name: Restore dependencies 
        run: dotnet restore

      - name: Build
        run: dotnet build

      - name: Run Xunit Test
        run: dotnet test